/**
 * @param {number[]} nums
 * @return {number}
 */
var missingNumber = function (nums) {
  //range will be [0, number]
  let number = nums.length;

  const allNumbers = new Array(0, number);
  //can I make an array of integer from 0 to number?
  //ex>
};
