// const visited = new Array(image.length)
//   .fill()
//   .map(() => new Array(image[0].length).fill(false));

// const visited = Array.from({ length: image.length }, () =>
//   Array(image[0].length).fill(false)
// );

// Array.from - a built-in JS function-create a new aaray based on iterable such as an array-like object or string or a generatoe function

// Array.from()-create array by specifying the length
// mapping functions is to populate the array ElementInternals
