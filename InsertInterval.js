/**
 * @param {number[][]} intervals
 * @param {number[]} newInterval
 * @return {number[][]}
 */
var insert = function (intervals, newInterval) {
  //non-overlapping??
  //an array named intervals - sorted ascending order
  //there is another array named "newInterval"
  //insert newInterval to intervals- needed to be sorted ascending order
};
